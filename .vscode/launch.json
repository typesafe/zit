{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "init",
      "type": "lldb",
      "request": "launch",
      "cwd": "${workspaceRoot}/test_dir",
      "program": "zig",
      "args": ["run", "${workspaceRoot}/src/main.zig", "--", "init"]
    },
  ]
}
